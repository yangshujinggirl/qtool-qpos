import React from 'react';
import { connect } from 'dva';


function Inventorydiff() {
  return (
    <div>
      Route Component: Inventorydiff
    </div>
  );
}

function mapStateToProps() {
  return {};
}

export default connect(mapStateToProps)(Inventorydiff);
